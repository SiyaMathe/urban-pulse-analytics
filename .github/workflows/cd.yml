name: CD — Deploy to Azure

on:
  push:
    branches: ["main"]
  workflow_dispatch:      # Allow manual triggers

env:
  DOTNET_VERSION: '8.0.x'
  NODE_VERSION: '20.x'
  AZURE_FUNCTIONAPP_PACKAGE_PATH: 'backend/functions'
  AZURE_WEBAPP_PACKAGE_PATH: 'backend/api'

jobs:
  # ─────────────────────────────────────────────────────────────────────────
  # Job 1: Deploy Azure Infrastructure via Bicep (IaC)
  # ─────────────────────────────────────────────────────────────────────────
  deploy-infrastructure:
    name: Deploy Infrastructure (Bicep)
    runs-on: ubuntu-latest
    environment: production
    outputs:
      functionapp-name: 'urban-pulse-functions'
      webapp-name:      'urban-pulse-api'
      sql-server-name:  'urban-pulse-sqlserver'

    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy Bicep infrastructure
        id: deploy
        uses: azure/arm-deploy@v2
        with:
          resourceGroupName: 'urban-pulse-rg'
          template: infrastructure/main.bicep
          parameters: >
            environment=production
            sqlAdminPassword=${{ secrets.SQL_ADMIN_PASSWORD }}
          failOnStdErr: false

  # ─────────────────────────────────────────────────────────────────────────
  # Job 2: Run Database Migrations
  # ─────────────────────────────────────────────────────────────────────────
  run-migrations:
    name: Run Database Migrations
    runs-on: ubuntu-latest
    needs: deploy-infrastructure
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Install sqlcmd
        run: |
          curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add -
          curl https://packages.microsoft.com/config/ubuntu/22.04/prod.list \
            | sudo tee /etc/apt/sources.list.d/msprod.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y mssql-tools unixodbc-dev

      - name: Run schema migrations (idempotent)
        run: |
          for f in backend/sql/migrations/V*.sql; do
            echo "Running migration: $f"
            /opt/mssql-tools/bin/sqlcmd \
              -S "urban-pulse-sqlserver.database.windows.net" \
              -U "cloudadmin" \
              -P "${{ secrets.SQL_ADMIN_PASSWORD }}" \
              -d "UrbanPulseAnalytics" \
              -i "$f" \
              -b    # Exit on error
          done

  # ─────────────────────────────────────────────────────────────────────────
  # Job 3: Build & Deploy Azure Functions
  # ─────────────────────────────────────────────────────────────────────────
  deploy-functions:
    name: Deploy Azure Functions
    runs-on: ubuntu-latest
    needs: [deploy-infrastructure, run-migrations]
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET